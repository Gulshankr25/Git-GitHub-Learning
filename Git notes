Git and Github 
git is software
github is a service (to host git online or on internet )

git is an open source version control system (used to track changes in file/folder )

github is a web-based hosting service for git repository, github is an online plateform to store and share your codes with others 

Repository is a collection of files and directories that are stored together. it is a way to store and manage your code. it is like a folder but more than a folder

git --version // to check current version of git
git status // to check status of repository

git config --global user.email "youremail@gmail.com"
git config --global user.name "your name"
git config --list //this will show you all the setting you have changed


>> workflow

{git init}--->[working directory]--->{git add}--->[staging area]--->{git commit}--->[ repo ]--->{git push}--->[ Github ]

>>stage
git init //create new git repository
git add <file> <file2> // add selected files in repo
git add . // add all files to repo at once
git status //to check repo status

>> commit
git commit -m "message" // to permanentaly store those staged changes into repository 

>>push
git push origin main //push on github

>> logs
git log  //show history of repository
git log --oneline  //show history of repository in oneline, or in short

>> change default code editor
git config --global core.editor "code --wait"

>> gitignore
gitignore is a file that tells git which file and folder to ignore. Basically the files under gitignore is not get track 


>> Branches in git
allow you to create a separate line of development that can be worked on independently of the main branch. This can be useful when you want to make changes to a project without affecting the main branch or when you want to work on a new feature or bug fix.

>>Head in git
head is a pointer to the current branch that you are working on. It points to the latest commit in the current branch. when you create a new branch it is automatically set as the head of that branch

>> creating new branches
git branch
git branch <branchname>
git switch <branchname>
git log
git switch -c <branchname>
git checkout <branchname>

git branch  //This command lists all the branches in the current repository.

git branch red //This command creates a new branch called red.

git switch red //This command switches to the red branch.

git log //This command shows the commit history for the current branch.

git switch main //This command switches to the main branch.

git switch -c green //This command creates a new branch called green. the -c flag is used to create a new branch.

git checkout yellow //This command switches to the yellow branch.

>>merging branches
Merging is about bringing changes from one branch to another.
In Git we have two types of merges :
>Fast-Forward Merges (If branches have not diverged)
>3-Way Merges (if branches have diverged)

>>fast forward merges
This one is easy as branch that you are trying to merge is usually ahead and there are no conflicts.
When you are done working on a branch, you can merge it back into the main branch.

git checkout <branchname>
git merge <branchname>

git checkout main - This command switches to the main branch.
git merge red - This command merges the red branch into the main branch

>> 3-way merge
In this type of merge, the main branch has additional commits that are not present in the red branch. This is not a fast-forward merge. Here git looks at 3 different commits [common ancestor of branches + tips of each branch] and combines the changes into one merge commit.

>>managing conflicts
there are no shortcuts to resolve conflicts. You have to manually resolve the conflicts. Decide, what to keep and what to discard. VSCode has a built-in merge tool that can help you resolve the conflicts.

>> Renaming a branch
git branch -m <old-branch-name> <new-branch-name>

git branch -m black white // the branch name black change in white

>>Delete a branch 
git branch -d <branchname>

git branch -d white // branch white is deleted

>>checkout a branch
git checkout <branchname>

git checkout main //get in main branch


>> merging Branches
Merging is about bringing changes from one branch to another.
In Git we have two types of merges :
>Fast-Forward Merges (If branches have not diverged)
>3-Way Merges (if branches have diverged)

>>fast forward merge
This one is easy as branch that you are trying to merge is usually ahead and there are no conflicts.
When you are done working on a branch, you can merge it back into the main branch. This is done using the following command

git checkout <branch>
git merge <branch>

git checkout main // This command switches to the main branch.
git merge red // This command merges the red branch into the main branch.

>>Diff, Stash and Tags
>git diff
The git diff is an informative command that shows the differences between two commits. It is used to compare the changes made in one commit with the changes made in another commit. Git consider the changed versions of same file as two different files. Then it gives names to these two files and shows the differences between them.

>How to Read the Diff Output
a/ – the original file (before changes)
b/ – the updated file (after changes)
--- – marks the beginning of the original file
+++ – marks the beginning of the updated file
@@ – shows the line numbers and position of changes

Here the file A and file B are the same file but different versions.

Git will show you the changes made in the file A and file B. It will also show you the line number where the change occurred along with little preview of the change.

git deff //This command shows the unstaged changes in your working directory compared to the staging area.

git diff --staged //This command shows the changes between your last commit and the staging area

git diff <branch-name-one> <branch-name-two> //This command compares the difference between two branches.

git diff branch-name-one..branch-name-two //Another way to compare the difference between two branches 

git diff <commit-hash-one> <commit-hash-two> //This command compares the difference between two commits.

>git stash
Stash is a way to save your changes in a temporary location. It’s useful when switching branches without losing work.

git stash //Stash is a way to save your changes in a temporary location.

-naming stash
git stash save "work in progress on X feature" //You can also name the stash
-view the stash list
git stash list 
-apply most recent stash
git stash apply 
-apply specific stash
git stash apply stash@{0} //Here stash@{0} is the name of the stash
-apply and drop a stash
git stash pop
-drop the stash
git stash drop
-applying stash to specific branch
git stash apply stash@{0} <branch-name>
-clearing the stash
git stash clear

>Git Tags
Tags are a way to mark a specific point in your repository. They are useful when you want to remember a specific version of your code or when you want to refer to a specific commit. Tags are like sticky notes that you can attach to your commits.

git tag <tag-name> //create a tag
git tag -a <tag-name> -m "message" //create a annotated tag
git tag //list all tags
git tag <tag-name> <commit-hash> //tagging a specific commit
git push origin <tag-name> //push tag to remote repository
git tag -d <tag-name> //delete a tag
git push origin :<tag-name> //delete tag on remote repository

>>Managing History
>Merge commits
A merge commit is a commit that combines two or more commits into one. It is created when you merge two or more branches into a single branch. The merge commit contains all the changes from the original branches, and it is used to keep the project history clean and easy to understand.

>Rebase in git
Git rebase is a powerful Git feature used to change the base of a branch. It effectively allows you to move a branch to a new starting point, usually a different commit, by “replaying” the commits from the original base onto the new base. This can be useful for keeping a cleaner, linear project history.

>>*Ensure you are on the branch you want to rebase <<*
git checkout <branchname> //bramnchname you want to rebase
git rebase <branchname> //branchname which rebase with
>This will replay the commits from feature-branch on top of the latest changes in main.

git checkout pink
git rebase main
>Resolve any conflicts
If there are any conflicts, you will need to resolve them manually. You can use the merge tool in VSCode to resolve the conflicts.

git add <resolved-files>
git rebase --continue

>Git reflog
Git reflog is a command that shows you the history of your commits. It allows you to see the changes that you have made to your repository over time. This can be useful for debugging and understanding the history of your project.

-view reflog
git reflog // show history of your commits
-find specific commiit
git reflog <commit-hash>

>Recover lost commits or changes
If you accidentally deleted a branch or made changes that are no longer visible in the commit history, you can often recover them using the reflog. First, find the reference to the commit where the branch or changes existed, and then reset your branch to that reference.

git reflog <commit-hash>
git reset --hard <commit-hash>

>**or you can use HEAD@{n} to reset to the nth commit before the one you want to reset to.*

git reflog <commit-hash>
git reset --hard HEAD@{n} //n= nth commit before like 2 commit before

>>What is GitHub?
Github is a web-based Git repository hosting service. It is a popular platform for developers to collaborate on projects and to share code. Github provides a user-friendly interface for managing and tracking changes to your code, as well as a platform for hosting and sharing your projects with others.

>Some other alternative of Github are:
Gitlab
Bitbucket
Azure Repos
Gitea
But mainstream popular tool these days is Github.

Creating a Github account is free and easy.Once you have created an account, you can start using Github to host and collaborate on your projects. Github provides a variety of features and tools that make it easy to manage and track your code, including issues, pull requests, and code reviews.

>>Publish Code to Remote Repository
Create a new Repo on your system first, add some code and commit it.

git init
git add <files>
git commit -m "commit message"

>remote url setting
git remote -v //show you the remote url of your repository.

>Add Remote Repository
git remote add origin <remote-url>

Here <remote-url> is the url of the remote repository that you want to add and origin is the name of the remote repository. This origin is used to refer to the remote repository in the future.

>Pushing code
git push <remote-name> <branch-name>

Here <remote-name> is the name of the remote repository that you want to push to and <branch-name> is the name of the branch that you want to push.

>Setup an upstream remote
Setting up an upstream remote is useful when you want to keep your local repository up to date with the remote repository. It allows you to fetch and merge changes from the remote repository into your local repository.

git remote add upstream <remote-url>

or you can use shorthand:

git remote add -u <remote-url>

You can do this at the time of pushing your code to the remote repository.

git push -u origin main

This will set up an upstream remote and push your code to the remote repository.

>>Get code from remote repository
There are two ways to get code from a remote repository:
>fetch the code
Fetch the code means that you are going to download the code from the remote repository to your local repository.

git fetch <remote-name>

>pull the code
Pull the code means that you are going to download the code from the remote repository and merge it with your local repository.

git pull <remote-name> <branch-name>

git pull origin main



ls //stands for "list", used to display files and directories
dir  //shows all directories
ls -force // show all files, including hidden files too

{
    cd .<filename> // to get in that directory 
    cd ..  // to go to parent directory(or one level up from current directory)
}

{
    when you have make changes in a file of x-branch but you want to save that changes in file of y-branch, then you decide to switch to y-branch but because of uncommited changes it restrict you to switch your branch then you can use following commands:

    git stash 
    git switch <branchname> //example:y-branch
    git stash pop

    this transfer your uncommited changes to y-branch

}